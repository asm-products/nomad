require 'rails_helper'

RSpec.describe PlacesController, :type => :controller do
  describe "GET index" do
    context "when there is at least one place" do
    end

    context "when there is no place" do
    end
  end
end
